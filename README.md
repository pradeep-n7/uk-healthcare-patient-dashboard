# UK Healthcare Patient Dashboard — Simple Sample

Simple React + TypeScript + Vite frontend for T-001 Patient Dashboard UI Development.

Includes patient summary, healthcare KPI cards, navigation, appointments, recent activity, and responsive layout.

Run:
npm install
npm run dev

Build:
npm run build

Demo data only; no real patient records, APIs, or database are used.
